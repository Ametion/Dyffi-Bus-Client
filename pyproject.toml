[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dyffi-client"
version = "0.1.0"
authors = [
    {name = "Yehor Kochetov", email = "yehor.kochetov@gmail.com"}
]
description = "A async Pub/Sub client for Dyffi-Bus"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.6"
dependencies = [
    "requests",
    "websocket-client"
]

[project.urls]
Homepage = "https://github.com/Ametion/dyffi-client"
Repository = "https://github.com/Ametion/dyffi-client"
